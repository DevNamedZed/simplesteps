{
  "name": "simplesteps-monorepo",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cdk"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "playground": "npm run dev --prefix playground"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.11.30",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.3"
  }
}
